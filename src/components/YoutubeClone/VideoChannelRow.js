import React from 'react'

function VideoChannelRow({ title, views, timestamp, channelImage, channel, image  }) {
  return (
    <div className='VideoChannelRow'>
        VideoChannelRow
        </div>
  )
}

export default VideoChannelRow